# Behavior Tree Tutorial
The implementation of the Behaviour Tree Editor as the plugin for Unity.

It has all base types of the nodes with some particular implementations. Also, it provides the feature of highlighting the state of the node, and, therefore, the aciive path in the tree. And the editor gives the opportunity to associate the particular blackboard with a certain tree. 
